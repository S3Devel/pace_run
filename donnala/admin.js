function adminPage(){if(!S.session){go('/admin/login');return ''}if(S.profile?.role!=='admin')return pageWrap('<section class="section"><div class="notice err">Acesso administrativo não autorizado.</div></section>');if(S.profile.must_change_password){setTimeout(()=>passwordModal(true),50)}let tabs=[['dashboard','Dashboard'],['products','Produtos'],['orders','Pedidos'],['delivery','Bairros/Entrega'],['schedule','Agenda'],['coupon','Cupom'],['banners','Banners'],['payments','Pagamentos'],['settings','Configurações'],['qr','QR Code'],['audit','Auditoria']];return `${header()}<main class="adminShell"><div class="adminTop"><div><span class="eyebrow" style="color:var(--wine)">ADMINISTRAÇÃO</span><h1>Donnalá</h1></div><button class="btn btn-outline" onclick="logout()">Sair</button></div><div class="adminTabs">${tabs.map(([k,l])=>`<button class="${S.adminTab===k?'active':''}" onclick="S.adminTab='${k}';render()">${l}</button>`).join('')}</div><div id="adminContent">${adminContent()}</div></main>${footer()}`}
function adminContent(){let a=S.admin;switch(S.adminTab){case'dashboard':{let ord=a.orders.filter(o=>o.status!=='cancelado'),rev=ord.reduce((x,o)=>x+o.total_cents,0),ticket=ord.length?rev/ord.length:0,sales={};ord.forEach(o=>(o.order_items||[]).forEach(i=>sales[i.name]=(sales[i.name]||0)+i.quantity));let top=Object.entries(sales).sort((x,y)=>y[1]-x[1]).slice(0,6);return `<div class="statgrid"><div class="stat"><span>Clientes</span><b>${a.profiles.filter(p=>p.role!=='admin').length}</b></div><div class="stat"><span>Pedidos</span><b>${ord.length}</b></div><div class="stat"><span>Faturamento</span><b>${money(rev)}</b></div><div class="stat"><span>Ticket médio</span><b>${money(ticket)}</b></div></div><div class="panel" style="margin-top:18px"><h3>Mais vendidos</h3>${top.length?top.map(([n,q])=>`<p class="row between"><span>${esc(n)}</span><b>${q} un.</b></p>`).join(''):'<p class="muted">Sem dados ainda.</p>'}</div>`}case'products':return `<div class="row between wrap" style="margin-top:18px"><h2 style="font-size:31px">Produtos</h2><button class="btn btn-primary" onclick="editProduct()">Novo produto</button></div><div class="tableWrap"><table class="table"><tr><th>Produto</th><th>Categoria</th><th>Preço</th><th>Estoque</th><th>Destaque</th><th>Ação</th></tr>${a.products.map(p=>`<tr><td>${esc(p.name)}</td><td>${esc(p.category)}</td><td>${money(p.price_cents)}</td><td>${p.in_stock?'Disponível':'Esgotado'}</td><td>${p.featured?'Mais pedido':'—'}</td><td><button class="smallbtn" onclick="editProduct('${p.id}')">Editar</button></td></tr>`).join('')}</table></div>`;case'orders':return `<h2 style="font-size:31px">Pedidos</h2><div class="tableWrap"><table class="table"><tr><th>Nº</th><th>Cliente</th><th>Data</th><th>Total</th><th>Pagamento</th><th>Status pedido</th><th>Status pag.</th></tr>${a.orders.map(o=>`<tr><td>${esc(o.order_number)}</td><td>${esc(o.customer_name)}<br><span class="muted">${fmtPhone(o.customer_phone)}</span></td><td>${new Date(o.created_at).toLocaleDateString('pt-BR')}</td><td>${money(o.total_cents)}</td><td>${esc(({pix:'PIX',cartao:'Cartão',dinheiro:'Dinheiro'}[o.payment_method]||o.payment_method||''))}</td><td><select onchange="patchOrder('${o.id}','status',this.value)"><option>${o.status}</option>${['criado','recebido','confirmado','em_producao','pronto','concluido','cancelado'].filter(x=>x!==o.status).map(x=>`<option>${x}</option>`)}</select></td><td><select onchange="patchOrder('${o.id}','payment_status',this.value)"><option>${o.payment_status||'pendente'}</option>${['pendente','pago','cancelado'].filter(x=>x!==(o.payment_status||'pendente')).map(x=>`<option>${x}</option>`)}</select></td></tr>`).join('')}</table></div>`;case'delivery':return `<div class="row between wrap"><h2 style="font-size:31px">Bairros e entrega</h2><button class="btn btn-primary" onclick="editNeighborhood()">Adicionar bairro</button></div><div class="tableWrap"><table class="table"><tr><th>Bairro</th><th>Taxa</th><th>Ativo</th><th></th></tr>${a.neighborhoods.map(n=>`<tr><td>${esc(n.name)}</td><td>${money(n.fee_cents)}</td><td>${n.active?'Sim':'Não'}</td><td><button class="smallbtn" onclick="editNeighborhood('${n.id}')">Editar</button></td></tr>`).join('')}</table></div><div class="panel" style="margin-top:16px"><h3>Pedidos mínimos</h3>${settingsMinimumForm()}</div>`;case'schedule':return `<div class="row between wrap"><h2 style="font-size:31px">Agenda</h2><button class="btn btn-primary" onclick="editDay()">Configurar data</button></div><div class="tableWrap"><table class="table"><tr><th>Data</th><th>Capacidade</th><th>Bloqueada</th><th>Nota</th><th></th></tr>${a.schedule.map(d=>`<tr><td>${String(d.day).split('-').reverse().join('/')}</td><td>${d.capacity}</td><td>${d.blocked?'Sim':'Não'}</td><td>${esc(d.note||'')}</td><td><button class="smallbtn" onclick="editDay('${d.day}')">Editar</button></td></tr>`).join('')}</table></div><div class="panel" style="margin-top:16px"><form onsubmit="saveDefaultCapacity(event)"><div class="field"><label>Capacidade padrão diária</label><input class="input" name="capacity" type="number" min="1" value="${S.settings?.default_daily_capacity||20}"></div><button class="btn btn-primary">Salvar</button></form></div>`;case'coupon':{let c=a.coupons.find(x=>x.code==='PRIMEIRA10')||a.coupons[0];return `<h2 style="font-size:31px">Cupom</h2><div class="panel"><form onsubmit="saveCoupon(event,'${c?.id||''}')"><div class="field"><label>Código</label><input class="input" name="code" value="${esc(c?.code||'PRIMEIRA10')}"></div><div class="field"><label>Percentual</label><input class="input" type="number" min="1" max="100" name="percent" value="${c?.percent_off||10}"></div><label class="row"><input type="checkbox" name="active" ${c?.active!==false?'checked':''}> Ativo</label><br><label class="row"><input type="checkbox" name="first" ${c?.first_purchase_only!==false?'checked':''}> Somente primeira compra</label><br><button class="btn btn-primary">Salvar cupom</button></form></div>`}case'banners':return `<div class="row between wrap"><h2 style="font-size:31px">Banners</h2><button class="btn btn-primary" onclick="editBanner()">Novo banner</button></div>${a.banners.map(b=>`<div class="order"><div class="row between"><div><b>${esc(b.title)}</b><div class="muted">${esc(b.subtitle||'')}</div></div><button class="smallbtn" onclick="editBanner('${b.id}')">Editar</button></div></div>`).join('')||'<div class="empty">Nenhum banner.</div>'}`;case'payments':return paymentSettingsForm();case'settings':return generalSettingsForm();case'qr':{let url=location.origin+location.pathname+'#/';return `<h2 style="font-size:31px">QR Code do cardápio</h2><div class="panel" style="text-align:center"><img alt="QR Code" style="max-width:280px;width:100%" src="https://api.qrserver.com/v1/create-qr-code/?size=350x350&data=${encodeURIComponent(url)}"><p class="muted">${esc(url)}</p><a class="btn btn-primary" target="_blank" href="https://api.qrserver.com/v1/create-qr-code/?size=900x900&data=${encodeURIComponent(url)}">Abrir para imprimir</a></div>`}case'audit':return `<h2 style="font-size:31px">Auditoria</h2><div class="tableWrap"><table class="table"><tr><th>Data</th><th>Ação</th><th>Entidade</th><th>ID</th></tr>${a.audit.map(x=>`<tr><td>${new Date(x.created_at).toLocaleString('pt-BR')}</td><td>${esc(x.action)}</td><td>${esc(x.entity_type)}</td><td>${esc(x.entity_id||'')}</td></tr>`).join('')}</table></div>`;default:return''}}
function settingsMinimumForm(){return `<form onsubmit="saveMinimums(event)"><div class="adminForm"><div class="field"><label>Mínimo entrega (R$)</label><input class="input" name="delivery" inputmode="decimal" value="${(S.settings?.min_delivery_cents||0)/100}"></div><div class="field"><label>Mínimo retirada (R$)</label><input class="input" name="pickup" inputmode="decimal" value="${(S.settings?.min_pickup_cents||0)/100}"></div></div><button class="btn btn-primary">Salvar</button></form>`}
function paymentSettingsForm(){let s=S.settings||{};return `<h2 style="font-size:31px">Pagamentos</h2><div class="panel"><form onsubmit="savePayments(event)"><label class="row"><input type="checkbox" name="pix" ${s.pay_pix!==false?'checked':''}> PIX</label><br><label class="row"><input type="checkbox" name="card" ${s.pay_card!==false?'checked':''}> Cartão presencial</label><br><label class="row"><input type="checkbox" name="cash" ${s.pay_cash!==false?'checked':''}> Dinheiro</label><div class="field"><label>Chave PIX (opcional)</label><input class="input" name="pixkey" value="${esc(s.pix_key||'')}" placeholder="Não preencher até definir a chave oficial"></div><div class="field"><label>Favorecido PIX (opcional)</label><input class="input" name="holder" value="${esc(s.pix_holder||'')}"></div><button class="btn btn-primary">Salvar pagamentos</button></form></div>`}
function generalSettingsForm(){let s=S.settings||{};return `<h2 style="font-size:31px">Configurações</h2><div class="panel"><form onsubmit="saveGeneral(event)"><div class="field"><label>WhatsApp</label><input class="input" name="wa" value="${esc(s.whatsapp_number||WA)}"></div><label class="row"><input type="checkbox" name="hide" ${s.hide_unavailable?'checked':''}> Ocultar produtos indisponíveis no catálogo</label><br><button class="btn btn-primary">Salvar configurações</button></form></div>`}
async function patch(table,id,data,key='id'){await api(`/rest/v1/${table}?${key}=eq.${encodeURIComponent(id)}`,{method:'PATCH',token:S.session.access_token,body:data,headers:{Prefer:'return=minimal'}})}async function insert(table,data){return api('/rest/v1/'+table,{method:'POST',token:S.session.access_token,body:data,headers:{Prefer:'return=representation'}})}async function refreshAdmin(){await loadPublic();await loadAdmin();render()}
async function patchOrder(id,k,v){try{await patch('orders',id,{[k]:v});toast('Pedido atualizado','ok');await refreshAdmin()}catch(e){toast(e.message,'err')}}
function editProduct(id){let p=S.admin.products.find(x=>x.id===id)||{};modal.classList.add('on');modal.innerHTML=`<div class="modalBox"><div class="row between"><h3>${id?'Editar':'Novo'} produto</h3><button class="close" onclick="closeModal()">×</button></div><form onsubmit="saveProduct(event,'${id||''}')"><div class="adminForm"><div class="field"><label>Nome</label><input class="input" name="name" value="${esc(p.name||'')}" required></div><div class="field"><label>Categoria</label><select class="input" name="category"><option ${p.category==='fatias'?'selected':''}>fatias</option><option ${p.category==='tortas'?'selected':''}>tortas</option><option ${p.category==='bebidas'?'selected':''}>bebidas</option></select></div><div class="field"><label>Preço (R$)</label><input class="input" name="price" value="${p.price_cents!=null?p.price_cents/100:''}" required></div><div class="field"><label>Tag</label><input class="input" name="tag" value="${esc(p.tag||'')}"></div><div class="field full"><label>Descrição</label><textarea class="input" name="description">${esc(p.description||'')}</textarea></div><div class="field full"><label>URL da imagem</label><input class="input" name="image" value="${esc(p.image_url||'')}"><small class="muted">Ou envie uma imagem abaixo.</small><input style="margin-top:8px" type="file" name="file" accept="image/jpeg,image/png,image/webp"></div><div><label><input type="checkbox" name="active" ${p.active!==false?'checked':''}> Ativo</label></div><div><label><input type="checkbox" name="stock" ${p.in_stock!==false?'checked':''}> Disponível</label></div><div><label><input type="checkbox" name="featured" ${p.featured?'checked':''}> Mais pedido</label></div><div class="field"><label>Ordem</label><input class="input" type="number" name="sort" value="${p.sort_order||0}"></div></div><button class="btn btn-primary">Salvar produto</button></form></div>`}
async function uploadProductImage(file){if(!file||!file.size)return null;let path=`${Date.now()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'-')}`;let r=await fetch(`${SB}/storage/v1/object/product-images/${path}`,{method:'POST',headers:{apikey:KEY,Authorization:'Bearer '+S.session.access_token,'Content-Type':file.type,'x-upsert':'true'},body:file});if(!r.ok)throw new Error('Falha ao enviar imagem');return `${SB}/storage/v1/object/public/product-images/${path}`}
async function saveProduct(e,id){e.preventDefault();let f=new FormData(e.target);try{let image=String(f.get('image')||''),file=f.get('file');if(file?.size)image=await uploadProductImage(file);let data={name:String(f.get('name')).trim(),category:f.get('category'),description:String(f.get('description')||''),price_cents:Math.round(parseFloat(String(f.get('price')).replace(',','.'))*100),image_url:image||null,tag:String(f.get('tag')||'')||null,active:f.get('active')==='on',in_stock:f.get('stock')==='on',featured:f.get('featured')==='on',sort_order:+f.get('sort')||0};if(id)await patch('products',id,data);else{data.slug=(data.name.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'')+'-'+Date.now());await insert('products',data)}closeModal();toast('Produto salvo','ok');await refreshAdmin()}catch(x){toast(x.message,'err')}}
function editNeighborhood(id){let n=S.admin.neighborhoods.find(x=>x.id===id)||{};modal.classList.add('on');modal.innerHTML=`<div class="modalBox"><div class="row between"><h3>Bairro</h3><button class="close" onclick="closeModal()">×</button></div><form onsubmit="saveNeighborhood(event,'${id||''}')"><div class="field"><label>Nome</label><input class="input" name="name" value="${esc(n.name||'')}" required></div><div class="field"><label>Taxa (R$)</label><input class="input" name="fee" value="${n.fee_cents!=null?n.fee_cents/100:0}" required></div><label><input type="checkbox" name="active" ${n.active!==false?'checked':''}> Ativo</label><br><br><button class="btn btn-primary">Salvar</button></form></div>`}async function saveNeighborhood(e,id){e.preventDefault();let f=new FormData(e.target),data={name:String(f.get('name')).trim(),fee_cents:Math.round(parseFloat(String(f.get('fee')).replace(',','.'))*100),active:f.get('active')==='on'};try{id?await patch('neighborhoods',id,data):await insert('neighborhoods',data);closeModal();await refreshAdmin();toast('Bairro salvo','ok')}catch(x){toast(x.message,'err')}}
function editDay(day){let d=S.admin.schedule.find(x=>x.day===day)||{};modal.classList.add('on');modal.innerHTML=`<div class="modalBox"><div class="row between"><h3>Configurar data</h3><button class="close" onclick="closeModal()">×</button></div><form onsubmit="saveDay(event,'${day||''}')"><div class="field"><label>Data</label><input class="input" type="date" name="day" value="${d.day||''}" ${day?'readonly':''} required></div><div class="field"><label>Capacidade</label><input class="input" type="number" min="1" name="cap" value="${d.capacity||S.settings?.default_daily_capacity||20}"></div><label><input type="checkbox" name="blocked" ${d.blocked?'checked':''}> Bloqueada</label><div class="field"><label>Nota</label><input class="input" name="note" value="${esc(d.note||'')}"></div><button class="btn btn-primary">Salvar</button></form></div>`}async function saveDay(e,old){e.preventDefault();let f=new FormData(e.target),data={day:f.get('day'),capacity:+f.get('cap'),blocked:f.get('blocked')==='on',note:String(f.get('note')||'')||null};try{if(old)await patch('schedule_days',old,data,'day');else await insert('schedule_days',data);closeModal();await refreshAdmin();toast('Agenda atualizada','ok')}catch(x){toast(x.message,'err')}}
async function saveDefaultCapacity(e){e.preventDefault();await saveSettings({default_daily_capacity:+new FormData(e.target).get('capacity')})}async function saveMinimums(e){e.preventDefault();let f=new FormData(e.target);await saveSettings({min_delivery_cents:Math.round(parseFloat(String(f.get('delivery')).replace(',','.'))*100)||0,min_pickup_cents:Math.round(parseFloat(String(f.get('pickup')).replace(',','.'))*100)||0})}async function savePayments(e){e.preventDefault();let f=new FormData(e.target);await saveSettings({pay_pix:f.get('pix')==='on',pay_card:f.get('card')==='on',pay_cash:f.get('cash')==='on',pix_key:String(f.get('pixkey')||'')||null,pix_holder:String(f.get('holder')||'')||null})}async function saveGeneral(e){e.preventDefault();let f=new FormData(e.target);await saveSettings({whatsapp_number:digits(f.get('wa'))||WA,hide_unavailable:f.get('hide')==='on'})}async function saveSettings(data){try{await patch('app_settings','true',data,'id');await refreshAdmin();toast('Configuração salva','ok')}catch(x){toast(x.message,'err')}}
async function saveCoupon(e,id){e.preventDefault();let f=new FormData(e.target),data={code:String(f.get('code')).toUpperCase(),percent_off:+f.get('percent'),active:f.get('active')==='on',first_purchase_only:f.get('first')==='on'};try{id?await patch('coupons',id,data):await insert('coupons',data);await refreshAdmin();toast('Cupom salvo','ok')}catch(x){toast(x.message,'err')}}
function editBanner(id){let b=S.admin.banners.find(x=>x.id===id)||{};modal.classList.add('on');modal.innerHTML=`<div class="modalBox"><div class="row between"><h3>Banner</h3><button class="close" onclick="closeModal()">×</button></div><form onsubmit="saveBanner(event,'${id||''}')"><div class="field"><label>Título</label><input class="input" name="title" value="${esc(b.title||'')}" required></div><div class="field"><label>Subtítulo</label><input class="input" name="subtitle" value="${esc(b.subtitle||'')}"></div><div class="field"><label>CTA</label><input class="input" name="cta" value="${esc(b.cta_label||'')}"></div><div class="field"><label>Link CTA</label><input class="input" name="url" value="${esc(b.cta_url||'#/catalogo')}"></div><div class="field"><label>Imagem URL</label><input class="input" name="image" value="${esc(b.image_url||'')}"></div><div class="adminForm"><div class="field"><label>Início</label><input class="input" type="date" name="start" value="${b.starts_at||''}"></div><div class="field"><label>Fim</label><input class="input" type="date" name="end" value="${b.ends_at||''}"></div></div><label><input type="checkbox" name="active" ${b.active!==false?'checked':''}> Ativo</label><br><br><button class="btn btn-primary">Salvar</button></form></div>`}async function saveBanner(e,id){e.preventDefault();let f=new FormData(e.target),data={title:String(f.get('title')),subtitle:String(f.get('subtitle')||''),cta_label:String(f.get('cta')||'')||null,cta_url:String(f.get('url')||'')||null,image_url:String(f.get('image')||'')||null,starts_at:f.get('start')||null,ends_at:f.get('end')||null,active:f.get('active')==='on'};try{id?await patch('banners',id,data):await insert('banners',data);closeModal();await refreshAdmin();toast('Banner salvo','ok')}catch(x){toast(x.message,'err')}}
async function render(){await maybeRefresh();let r=route();if(r==='/entrar'){root.innerHTML=loginPage(false);return}if(r==='/cadastro'){root.innerHTML=signupPage();return}if(r==='/politica-de-privacidade'){root.innerHTML=privacy();return}if(r==='/minha-conta'){await loadOrders();root.innerHTML=account();return}if(r==='/admin/login'){root.innerHTML=loginPage(true);return}if(r==='/admin'){if(!S.profile&&S.session)await loadProfile();if(S.profile?.role==='admin')await loadAdmin();root.innerHTML=adminPage();return}root.innerHTML=home();if(r==='/catalogo')setTimeout(()=>document.getElementById('catalogo')?.scrollIntoView(),30);updateCartBadge()}
window.addEventListener('hashchange',render);(async()=>{await maybeRefresh();await loadPublic();if(S.session)await loadProfile();await render();})();
